import React from 'react';

const toolbar = (props) => {
    return (
        <header>
            <div>MENU</div>
            <div>LOGO</div>
            <nav>
                ...
            </nav>
        </header>
    );
}

export default toolbar;